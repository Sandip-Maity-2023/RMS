import React from 'react';

const OrderReturn = () => (
  <section style={{ backgroundColor: '#fff', borderRadius: '8px', marginTop: '20px', padding: '24px' }}>
    <h2 style={{ marginTop: 0 }}>Order Returns</h2>
    <p>Use rental orders to process returns, late fees, and deposit settlement.</p>
  </section>
);

export default OrderReturn;
